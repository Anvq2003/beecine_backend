const myCustomLabels = {
  totalDocs: 'totalResults',
  docs: 'data',
  totalPages: 'totalPages',
  pagingCounter: 'slNo',
  meta: 'info',
};

const config = {
  myCustomLabels,
};

module.exports = config;
